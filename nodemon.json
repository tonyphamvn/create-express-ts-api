{
    "verbose": true,
    "watch": ["src"],
    "ext": "ts",
    "exec": "ts-node -r tsconfig-paths/register ./src/index.ts",
    "env": {
        "NODE_ENV": "development",
        "PORT": "4000"
    }
}
